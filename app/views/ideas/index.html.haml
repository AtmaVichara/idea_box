- if current_user
  .new-idea
    = link_to "New Idea", new_user_idea_path(current_user), :class => "new-button"

%h1 Ideas

.ideas
  - @ideas.each do |idea|
    %h3
      = link_to "#{idea.title}", user_idea_path(idea.user, idea)
