.admin-create
  = link_to "Create Category", new_admin_category_path, :class => 'new-button'

%h1 All Categories

.categories
  - @categories.each do |category|
    %h3
      = link_to "#{category.name}", admin_category_path(category)
